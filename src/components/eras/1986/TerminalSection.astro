---
interface Props {
  text: string;
  instant?: boolean;
}

const { text, instant = false } = Astro.props;
---
<pre
  class="bbs-pre typewriter"
  data-text={text}
  data-instant={instant ? 'true' : 'false'}
  data-typed="false"
></pre>
